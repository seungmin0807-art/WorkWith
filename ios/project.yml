name: WorkWith
options:
  createIntermediateGroups: true
  minimumXcodeGenVersion: 2.38.0

settings:
  base:
    PRODUCT_NAME: WorkWith
    PRODUCT_BUNDLE_IDENTIFIER: com.seungmin0807.workwith
    MARKETING_VERSION: 1.0.0
    CURRENT_PROJECT_VERSION: 1
    IPHONEOS_DEPLOYMENT_TARGET: 16.0
    TARGETED_DEVICE_FAMILY: 1
    GENERATE_INFOPLIST_FILE: YES
    INFOPLIST_KEY_CFBundleDisplayName: WorkWith
    INFOPLIST_KEY_NSCameraUsageDescription: WorkWith는 운동 분석 중 카메라 사용 상태를 표시하기 위해 카메라 권한을 요청합니다.
    ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    CODE_SIGN_STYLE: Automatic

targets:
  WorkWith:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: WorkWithApp
        excludes:
          - Assets.xcassets
    resources:
      - path: WorkWithApp/Assets.xcassets
      - path: BundledApp
        type: folder

schemes:
  WorkWith:
    build:
      targets:
        WorkWith: all
